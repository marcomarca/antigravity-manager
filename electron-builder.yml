appId: com.antigravity.projectlauncher
productName: Antigravity Project Launcher
directories:
  output: release
  buildResources: resources

publish:
  provider: github
  owner: marcomarca
  repo: antigravity-manager

files:
  - "dist/**/*"
  - "resources/**/*"
  - "package.json"

win:
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64
  icon: resources/icon.ico

nsis:
  oneClick: true
  perMachine: false
  allowToChangeInstallationDirectory: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Antigravity Project Launcher
  artifactName: "Antigravity-Project-Launcher-Setup-${version}.${ext}"

portable:
  artifactName: "${productName} ${version} Portable.${ext}"
